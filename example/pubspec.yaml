name: example
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: "none"


environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  meta:
  json_annotation:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.1.1
  glob:
  lean_builder:
    path: ../../lean_builder



